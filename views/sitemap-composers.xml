<?xml version="1.0" encoding="utf-8" ?>
    <urlset xmlns="http://www.google.com/schemas/sitemap/0.9">
        <% (composers||[]).forEach(function(com, i) { %>
        <url>
            <loc><%= baseUrl %><%= global.helpers.url({ composerUri: com.get('uri') }) %></loc>
            <lastmod><%= com.get('updated').toISOString() %></lastmod>
            <changefreq>monthly</changefreq>
            <priority>0.7</priority>
        </url>
        <% }) %>
    </urlset>
